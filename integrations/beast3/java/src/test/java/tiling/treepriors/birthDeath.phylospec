Alignment data = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Tree tree ~ BirthDeath(
    diversificationRate~LogNormal(mean=1.0, logSd=0.5),
    turnover~LogNormal(mean=1.0, logSd=0.5),
    taxa=taxa(data)
)

// EXPECTED_TILES
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED BEAST STATE
// SN: tree, diversificationRate, turnover
// P: tree_prior, diversificationRate_prior, turnover_prior
// EXPECTED BEAST STATE