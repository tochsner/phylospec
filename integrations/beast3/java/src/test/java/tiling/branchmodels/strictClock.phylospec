Alignment alignment = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Taxa taxa = taxa(alignment)
Tree tree ~ Yule(
    birthRate=1.0, taxa
)
Distribution<Vector<Rate>> dist = StrictClock(clockRate=1.0, tree)

// EXPECTED_TILES
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED BEAST STATE
// SN: tree
// P: tree_prior
// EXPECTED BEAST STATE