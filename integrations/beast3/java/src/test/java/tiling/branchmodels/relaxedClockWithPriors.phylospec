Alignment alignment = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Taxa taxa = taxa(alignment)
Tree tree ~ Yule(
    birthRate=1.0, taxa
)

Distribution<Vector<Rate>> dist = RelaxedClock(
    clockRate~Gamma(shape=1.0, rate=2.0),
    base=LogNormal(mean=1.0, logSd=2.0),
    tree
)
Distribution<Vector<Rate>> dist = RelaxedClock(
    clockRate~LogNormal(logMean=1, logSd=2.0),
    base=Gamma(shape=1.0, rate=2.0),
    tree
)

// EXPECTED_TILES
// TILING_SUCCESS
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED_APPLICATION_ERRORS
// Base distribution used for the relaxed clock should have a mean of 1.0. You use a distribution with mean 0.5. Use a distribution with mean 1.0.
// EXPECTED_APPLICATION_ERRORS

// EXPECTED BEAST STATE
// NO_STATE
// EXPECTED BEAST STATE